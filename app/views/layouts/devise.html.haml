!!!
%html
	= render 'layouts/head'
	%body	
		= render 'layouts/header'	
		.container
			.span5
				= yield
			.span6
				= image_tag('1.jpg', class: "img-rounded")
		%br
		.container
			= render 'devise/shared/slide'
		.container-fluid
			= render 'layouts/footer'
			= debug(params) if Rails.env.development?
